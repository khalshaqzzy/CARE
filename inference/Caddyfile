{
	admin off
	auto_https off
}

:30000 {
	@authorized header Authorization "Bearer {$INFERENCE_API_KEY}"
	handle @authorized {
		reverse_proxy inference:30000
	}
	respond `{ "error": "unauthorized"}` 401 {
		close
	}
}

title = "CARE Gitleaks configuration"

[extend]
useDefault = true

[allowlist]
description = "The root .env and .env.local are intentionally untracked mode-0600 local runtime secret files"
paths = ['''^/repo/\.env$''', '''^/repo/\.env\.local$''']
